$(document).ready(function() {
    let score;
    let time;

    console.log("logic loaded");

    // functions

    //create function which determines direction to head next based on user input
    //creat function which turns the next box black
    //create fucntion which checks to see if fucntion is already black aka biting itself
    //create timer to see how long you last
    //create timed function which calls for a dot to appear
    //create function which displays a dot to be eaten
    //create function which increases score when dot is eaten
});
